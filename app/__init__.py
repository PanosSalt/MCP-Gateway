"""MCP Gateway.

``__version__`` is the single source of truth for the running application's
version and is bumped automatically by python-semantic-release (see
``version_variables`` in pyproject.toml). Import it rather than hardcoding a
version string anywhere else.
"""

__version__ = "1.0.2"
